// In browsers, input can be taken using prompt(): let name = prompt("Enter your name");
// The input from prompt() is always a string, so convert if needed: let age = Number(prompt("Enter age"));
// For advanced input (like forms), use HTML input fields and access them via JavaScript DOM.
